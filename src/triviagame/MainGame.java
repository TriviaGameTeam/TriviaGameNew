package TriviaGame;

import TriviaGameGUI.Log;
import java.util.Scanner;

public class MainGame {

    public static void main(String[] args) throws Exception {
        Log log = new Log();
     /*   Scanner input = new Scanner(System.in);//יצירת משתנה לקלט
//        int level;//**כרגע אין שימוש ברמת המשחק**
        int countQuestionsAnswers;//יצירת משתנה שליו מוכנס כמות השאלות שהמשתמש רוצה לענות כרגע זה עד (5)
        boolean catchException = false; // משתנה שכל תפקידו לדאוג המשחק ירוץ תמיד

        do {
            System.out.println("Please enter the number of question that you want to answer(the max is 5):");
            countQuestionsAnswers = input.nextInt();
            //בדיקת תקינות הקלט מהמשתמש כאשר נזרקת חריגה במקרה הצורך
            if (countQuestionsAnswers < 1 || countQuestionsAnswers > 5) {
                throw new Exception ("NOT valid input!");
            } else {
                //playGame יצירת המשחק על ידי מימוש הבנאי בקלאס
                playGame pGame = new playGame(countQuestionsAnswers);
            }

        } while (!catchException);*/
    }
}
